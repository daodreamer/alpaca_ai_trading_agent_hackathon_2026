"""Impure support for the pure layers — clocks, exchange calendars, in-memory stores.

Extracted alongside `alphagate.core` from Personal Market Monitor. Everything here
is allowed to import third-party libraries; nothing in `core`, `options` or `risk`
may import from this package.
"""
